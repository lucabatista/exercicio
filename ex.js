window.onload = function(){
// const array1 = [5, 12, 8, 130, 44];

// let index = 2;

// console.log(`Using an index of ${index} the item returned is ${array1.at(index)}`);
// // expected output: "Using an index of 2 the item returned is 8"

// index = -2;

// console.log(`Using an index of ${index} item returned is ${array1.at(index)}`);
// // expected output: "Using an index of -2 item returned is 130"


                // ARRAY AT()
// NESTE EXERCICIO, DEFINIMOS UM ARRAY COM VARIOS VALORES NELE, E, LOGO DEPOIS UTILIZAMOS UMA VARIAVEL PAR DIZER QUAL INDICE QUEREMOS RESGATAR...

const arrayF = [22,300,45,78];
// let index = 3;
console.log(`se usar  o item vai retornar ${arrayF.at(index)}`);
}

